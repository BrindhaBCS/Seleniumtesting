---
- service: eat-jenkins          # Name of the service subscription
  teamKey: zzzz                 # Replace with EAT Jenkins Team Key for the given Jenkins instance (e.g. TABC)
  jenkinsType: dev              # EAT Jenkins Instance type. Supported values: dev, release
  environment: Production
